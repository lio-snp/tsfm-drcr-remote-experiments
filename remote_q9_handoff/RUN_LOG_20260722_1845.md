# E-GAP-000R External Recovery Run Log

## Machine

- Operator: Codex remote recovery executor
- Execution dates: 2026-07-20 through 2026-07-22 (Asia/Shanghai)
- Hostname: `liosnp`
- OS: Windows 11, build 26200, 64-bit
- CPU: 13th Gen Intel Core i7-13650HX, 20 logical processors
- RAM: 23.73 GiB
- GPU: NVIDIA GeForce RTX 4060 Laptop GPU, 8 GiB VRAM, driver 560.94
- Python: 3.12.3, conda-forge build, in `.venv-chronos`
- Branch: `main`
- Starting commit: `a7c8886e2fdb37ea4ec31d8dfd65c6b2c9b67f8f`
- Model/data cache root: `E:\DRCR-helper\tsfm-drcr-cache`

## Frozen Scope

- Recovery manifest: exactly 33 jobs from the 42-job external endpoint.
- Categories: 15 timeout, 3 baseline infeasible, 5 TimesFM non-finite,
  9 runner-complete contract mismatch, and 1 outcome-invalid.
- All recovery commands were executed through
  `scripts/run_benefit_selective_external_recovery.py` or its per-job recovery
  path, with `--force` and an 8-hour per-job timeout.
- No frozen manifest, model ID, window request, context cap, quantile grid,
  baseline definition, output slug, method, policy, action, metric, locked
  runner, or evaluator was edited.
- `--allow-low-memory` and compatibility shims were not used.

## Environment And Data

The existing isolated environment was used. To enable the frozen TimesFM
runner's built-in CUDA auto-selection, only the PyTorch environment package
was changed:

```powershell
.venv-chronos\Scripts\python.exe -m pip install --force-reinstall --no-deps `
  torch==2.4.1+cu121 --index-url https://download.pytorch.org/whl/cu121
```

The post-install probe reported PyTorch `2.4.1+cu121`, CUDA available, one
device, and `NVIDIA GeForce RTX 4060 Laptop GPU`. Loading the exact frozen
`google/timesfm-2.5-200m-pytorch` model selected `cuda:0`; `nvidia-smi`
confirmed the TimesFM Python process and approximately 1.3-1.7 GiB VRAM use.
Chronos and Moirai remained on CPU because their hash-locked runners/frozen
commands explicitly select or default to CPU.

All 14 required GIFT-Eval variants were present under `data/gift-eval`.
Datasets and model caches were not added to Git.

## Preparation And Validation Commands

```powershell
python scripts/build_benefit_selective_external_recovery_manifest.py
python tests/test_benefit_selective_external_recovery.py
python scripts/run_benefit_selective_external_recovery.py --dry-run

python scripts/run_benefit_selective_external_recovery.py --family chronos --timeout-seconds 28800
python scripts/run_benefit_selective_external_recovery.py --family moirai --timeout-seconds 28800
python scripts/run_benefit_selective_external_recovery.py --family timesfm --timeout-seconds 28800

python scripts/evaluate_benefit_selective_external.py --preflight-only
```

The manifest builder, one-test recovery suite, and dry-run passed. Dry-run
listed exactly 33 jobs: Chronos 10, Moirai 9, and TimesFM 14.

## Family Execution Summary

| Family | Attempted | Queue/artifact OK | Failed artifact contract | Evaluator-ready after run |
| --- | ---: | ---: | ---: | ---: |
| Chronos | 10 | 9 | 1 | 6 of the 10 recovered jobs |
| Moirai | 9 | 8 | 1 | 5 of the 9 recovered jobs |
| TimesFM | 14 | 8 | 6 | 0 of the 14 recovered jobs |
| Total | 33 | 25 | 8 | 11 of the 33 recovered jobs |

The last total is intentionally not inferred from wrapper `ok` values. The
final fail-closed evaluator inventory is authoritative and reports 20 of 42
endpoint jobs ready, versus 10 of 42 before recovery. The outcome-invalid
Chronos job was already artifact-ready in the earlier preflight, so the net
endpoint gain is 10 even though 11 recovery-manifest jobs are now ready.

## Recovery By Frozen Failure Category

| Original category | Jobs | Queue/artifact result | Final contract result |
| --- | ---: | --- | --- |
| timeout | 15 | 15 completed | Chronos/Moirai 10 ready; all 5 TimesFM jobs blocked by q9 mismatch |
| baseline infeasible | 3 | 3 failed again | 0 ready; frozen baseline has no feasible rows for `car_parts/M/short` |
| TimesFM non-finite | 5 | 5 failed again | 0 ready; status is `blocked_no_rows` |
| runner-complete contract mismatch | 9 | 9 returned `status=ok` | 0 ready; still 1/7/8 windows or TimesFM q9 mismatch |
| outcome-invalid | 1 | 1 completed | 1 ready (`chronos_temperature_rain_d_short`) |

## Job Outcomes

Chronos successes: `ett1_w_short` (33 s), `electricity_15t_short`
(27,842 s), `us_births_m_short` (73 s), `kdd_cup_2018_h_medium`
(25,010 s), `temperature_rain_d_short` (5,022 s), `m_dense_h_short`
(28,792 s), `sz_taxi_15t_short` (19,915 s), `bizitobs_l2c_5t_medium`
(3,465 s), and `bitbrains_fast_storage_5t_short` (12,318 s).
`car_parts_m_short` failed the frozen baseline/artifact contract.

Moirai successes: `ett1_w_short` (76 s), `electricity_15t_short`
(24,225 s), `us_births_m_short` (82 s), `kdd_cup_2018_h_medium`
(19,514 s), `m_dense_h_short` (22,770 s), `sz_taxi_15t_short`
(15,188 s), `bizitobs_l2c_5t_medium` (2,806 s), and
`bitbrains_fast_storage_5t_short` (9,769 s). `car_parts_m_short` failed the
frozen baseline/artifact contract. The first Moirai KDD process was terminated
by an execution-session interruption before writing an artifact; it was
retried through the same frozen `--force` recovery path and succeeded.

TimesFM successes at wrapper level: `m4_monthly_m_short` (477 s),
`electricity_15t_short` (19,093 s), `kdd_cup_2018_h_medium` (19,814 s),
`temperature_rain_d_short` (4,390 s), `m_dense_h_short` (23,436 s),
`sz_taxi_15t_short` (16,155 s), `bizitobs_l2c_5t_medium` (4,008 s), and
`bitbrains_fast_storage_5t_short` (10,210 s). The five frozen non-finite jobs
(`m4_yearly`, `ett1`, `hospital`, `us_births`, and `restaurant`) remained
`blocked_no_rows`; `car_parts` remained baseline-infeasible.

## Fail-Closed Preflight

Final preflight result:

```text
status=pending_external_artifacts
jobs_ready=20/42
windows_ready=320/672
outcomes_inspected=false
```

Ready by family is Chronos 10/14, Moirai 10/14, and TimesFM 0/14.
The 22 non-ready jobs comprise:

- 8 with missing or non-OK status: five TimesFM non-finite jobs and all three
  family variants of baseline-infeasible `car_parts`.
- 9 runner-complete contract mismatches: ETT1 produces 7/16 series for
  Chronos/Moirai, US births produces 1/16, BizITObs produces 7/16 for all
  families, and TimesFM M4 monthly produces 8/16.
- 5 additional TimesFM timeout recoveries that have 16 windows but fail the
  exact q9 check.

Every TimesFM `status=ok` payload reports quantile levels
`[0.0, 0.1, ..., 0.9]`, while the hash-locked evaluator requires exactly
`[0.1, ..., 0.9]`. This is a frozen runner/evaluator contract mismatch. The
status JSON was not manually changed.

Because preflight did not reach 42/42 and 672/672, the final evaluator was not
executed. This preserves the frozen fail-closed protocol and keeps external
outcomes uninspected.

## Hashes, Critics, And Tests

- Independent frozen hash check: expected 28, checked 28, OK 28, missing 0,
  mismatch 0.
- Recovery test: 1 test passed.
- External protocol critic: PASS.
- External execution-pack critic: PASS.
- External evaluator critic: PASS.
- Native `bash` maps to WSL on this Windows host and failed to start with
  `Bash/Service/CreateInstance/E_ACCESSDENIED`. The embedded Python body of
  each unmodified critic script was then executed verbatim with the project
  Python environment; all three passed.

## Returned Artifacts

- Raw forecasts and history sidecars:
  `results/raw_forecasts/external_v1_{chronos,moirai,timesfm}_*.csv`
- Runner status JSON:
  `results/raw_forecasts/external_v1_*_status.json`
- Window metrics: `results/window_metrics/external_v1_*_metrics.csv`
- Failure outputs: `results/failure_mining/external_v1_*`
- Per-job logs: `results/aaai_stress/external_logs/external_v1_*.log`
- Recovery snapshots:
  `results/aaai_stress/external_logs/benefit_selective_external_recovery_status_*.csv`
- Execution status:
  `results/aaai_stress/benefit_selective_external_execution_status.csv`
- Final recovery status:
  `results/aaai_stress/benefit_selective_external_recovery_status.csv`
- Fail-closed preflight:
  `results/aaai_stress/benefit_selective_external_confirmation_preflight.json`

No artifact required external storage; generated result files fit in Git.

## Required Lead-Machine Action

Do not ingest or evaluate this endpoint as complete. A protocol-owner decision
is required for the frozen contract contradictions: the TimesFM runner emits an
extra q0 level, several datasets cannot supply the frozen 16 distinct series,
and the frozen classical baseline is infeasible for `car_parts`. Resolving
these requires a new reviewed freeze or an authorized compatibility revision;
it cannot be repaired by this execution-only recovery task.
